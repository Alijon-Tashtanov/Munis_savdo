<!DOCTYPE html>
<html lang="uz">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create New Position</title>
    <link rel="stylesheet" href="styles.css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        
        .form-container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 800px;
            padding: 20px;
        }
        
        .form-header {
            background-color: #4caf50;
            padding: 15px;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            color: #ffffff;
        }
        
        .form-header h2 {
            margin: 0;
            text-align: center;
            font-size: 22px;
            font-weight: 600;
        }
        
        .employee-form {
            padding: 20px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            font-size: 14px;
            border: 1px solid #ddd;
            border-radius: 6px;
            box-sizing: border-box;
            /* Ensures padding is included in width */
        }
        
        .form-group input:focus,
        .form-group select:focus {
            border-color: #4caf50;
            outline: none;
            box-shadow: 0 0 4px rgba(76, 175, 80, 0.3);
        }
        
        .form-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 10px;
            border-top: 1px solid #ddd;
        }
        
        .submit-btn {
            background-color: #4caf50;
            color: #fff;
            border: none;
            padding: 12px 24px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
        }
        
        .cancel-btn {
            background-color: transparent;
            color: #f44336;
            border: none;
            padding: 12px 24px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            text-decoration: none;
        }
        
        .submit-btn:hover,
        .cancel-btn:hover {
            opacity: 0.9;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <div class="form-header">
            <h2>Lavozim yaratish</h2>
        </div>
        <form class="employee-form" action="/api/save-position" method="POST">
            <div class="form-group">
                <label for="full_name">Nomi</label>
                <input type="text" autofocus id="full_name" name="title" placeholder="Name" required />
            </div>
            <div class="form-group">
                <label for="permission_id">Ruxsat</label>
                <select id="permission_id" name="permission_id" required>
            <option value="">Tanlang</option>
            <% permissions.forEach(function(data) { %>
            <option value="<%= data.id %>"><%= data.name %></option>
            <% }); %>
          </select>
            </div>

            <div class="form-footer">
                <a href="/api/positions" class="cancel-btn">Bekor qilish</a>
                <button type="submit" class="submit-btn">Yuborish</button>
            </div>
        </form>
    </div>
</body>

</html>