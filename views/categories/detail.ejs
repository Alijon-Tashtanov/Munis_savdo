<!DOCTYPE html>
<html lang="uz">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Category edit</title>
    <link rel="stylesheet" href="styles.css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        
        .form-container {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 1200px;
            height: 600px;
        }
        
        .form-header {
            background-color: #4caf50;
            padding: 15px;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }
        
        .form-header h2 {
            color: #ffffff;
            margin: 0;
            text-align: center;
            font-size: 18px;
        }
        
        .appeal-form {
            padding: 20px;
        }
        
        .form-group {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
        }
        
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: calc(100% - 10px);
            padding: 15px;
            margin: 5px;
            font-size: 14px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        
        .form-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 10px;
            border-top: 1px solid #ddd;
        }
        
        .submit-btn {
            background-color: #4caf50;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
        }
        
        .cancel-btn {
            background-color: transparent;
            color: #f44336;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            font-size: 14px;
        }
        
        .submit-btn:hover,
        .cancel-btn:hover {
            opacity: 0.8;
        }
        
        .badge {
            display: inline-block;
            padding: 0.25em 0.5em;
            font-size: 0.875rem;
            font-weight: 600;
            color: #fff;
            border-radius: 0.25rem;
            text-align: center;
            white-space: nowrap;
            vertical-align: middle;
            line-height: 1;
        }
        /* Success badge (green) */
        
        .badge-success {
            background-color: #28a745;
        }
        /* Warning badge (yellow/orange) */
        
        .badge-warning {
            background-color: #ffc107;
            color: #212529;
            /* Dark text color for contrast */
        }
        /* Error badge (red) */
        
        .badge-danger {
            background-color: #dc3545;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <div class="form-header">
            <h2>Kategoriya tahrirlash</h2>
        </div>
        <form class="appeal-form" action="/api/update-category/<%= category.id %>" method="POST">
            <input type="hidden" name="method" value="PUT" />
            <div class="form-group">
                <input type="text" name="name" placeholder="Филиал" required value="<%= category.name %>" />
            </div>
            <div class="form-group">
                <select name="status" class="status-select">
                    <option value="1" <%= category.status === 1 ? 'selected' : '' %>><span class="badge badge-success">Faol</span></option>
                    <option value="0" <%= category.status === 0 ? 'selected' : '' %>><span class="badge badge-danger">Nofaol</span></option>
                </select>
            </div>
            <div class="form-footer">
                <a type="button" href="/api/categories" class="cancel-btn">Bekor qilish</a>
                <button type="submit" class="submit-btn">Yuborish</button>
            </div>
        </form>
    </div>
</body>

</html>